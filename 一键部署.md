# 一键部署 Firebase Functions

## 推荐：GitHub Actions（本机上传失败时用这个）

本机部署一直报 **Failed to make request to storage.googleapis.com** 时，用 **GitHub Actions** 在云端部署，不占本机网络。

**一键操作**：配置好 Secrets 后，**推送 main** 或 **Actions 页 Run workflow** 即部署。

👉 **详见 [DEPLOY_VIA_GITHUB_ACTIONS.md](./DEPLOY_VIA_GITHUB_ACTIONS.md)**（含 Secrets 配置、Base64 生成、触发方式）。

---

## 本地终端一条命令部署

在项目根目录执行：

```bash
cd /Users/niyutong/Desktop/AI聚合器开发方式Two && ./deploy-with-service-account.sh
```

**前提**：`proxy.env`、服务账号密钥已配置；本机能连上 `storage.googleapis.com`。若仍上传失败，请用上面的 **GitHub Actions**。

---

## deploy-cloudshell.zip 在哪？

- **路径**：`/Users/niyutong/Desktop/AI聚合器开发方式Two/deploy-cloudshell.zip`（项目根目录）  
- **生成**：`./pack-for-cloudshell.sh`  
- **用途**：仅 **Cloud Shell 部署** 时上传用；本地部署、GitHub Actions 都不需要。

---

## 总结

| 方式 | 命令 / 操作 |
|------|-------------|
| **GitHub Actions**（推荐，绕过本机网络） | 配好 Secrets → `git push` 或 Actions **Run workflow** |
| **本地** | `cd ... && ./deploy-with-service-account.sh` |
| **Cloud Shell** | 见 `DEPLOY_VIA_CLOUD_SHELL.md` |
