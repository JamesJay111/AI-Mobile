---
description: GemGPT development standards and patterns
alwaysApply: true
---

# GemGPT Development Standards

## Component Structure

All components should follow this pattern:

```typescript
// 1. Imports
import { useState } from 'react';
import { SomeIcon } from 'lucide-react';

// 2. Interface definitions
interface ComponentProps {
  isOpen: boolean;
  onClose: () => void;
}

// 3. Component with loading/error/success states
export function Component({ isOpen, onClose }: ComponentProps) {
  const [isLoading, setIsLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [data, setData] = useState<any>(null);

  // Handler functions
  const handleAction = async () => {
    setIsLoading(true);
    setError(null);
    try {
      // API call
    } catch (e: any) {
      setError(e.message);
    } finally {
      setIsLoading(false);
    }
  };

  if (!isOpen) return null;

  return (
    <>
      {/* Backdrop */}
      <div className="fixed inset-0 bg-black/40 z-40" onClick={onClose} />
      
      {/* Content */}
      <div className="fixed inset-x-0 bottom-0 z-50">
        {/* Error state */}
        {error && (
          <div className="p-4 bg-red-50 border border-red-200 rounded-lg">
            <p className="text-red-800">{error}</p>
            <button onClick={handleAction} className="mt-2 text-red-700 underline">
              Retry
            </button>
          </div>
        )}
        
        {/* Loading state */}
        {isLoading && (
          <div className="flex gap-2 p-4">
            <div className="w-2 h-2 bg-gray-400 rounded-full animate-bounce" />
            <div className="w-2 h-2 bg-gray-400 rounded-full animate-bounce delay-100" />
            <div className="w-2 h-2 bg-gray-400 rounded-full animate-bounce delay-200" />
          </div>
        )}
        
        {/* Success state */}
        {data && (
          <div className="p-4 bg-green-50 border border-green-200 rounded-lg">
            <p className="text-green-800">Success!</p>
          </div>
        )}
      </div>
    </>
  );
}
```

## UI State Requirements

Every interactive element MUST have:
- Default state
- Hover state: `hover:bg-gray-100`
- Active state: `active:scale-95`
- Disabled state: `disabled:opacity-50 disabled:cursor-not-allowed`
- Loading state: Show spinner + disable interaction

## API Integration Pattern

```typescript
// 1. Import service
import { chatCompletion } from '../services/openRouter';

// 2. Call in async handler
const handleSend = async () => {
  setIsLoading(true);
  setError(null);
  
  try {
    const response = await chatCompletion({
      messages: apiMessages,
      modelId: openRouterModelId,
      userId: 'current-user',
      isPro: isPro,
    });
    
    if (response.success) {
      // Handle success
    } else {
      throw new Error(response.error);
    }
  } catch (e: any) {
    setError(e.message);
  } finally {
    setIsLoading(false);
  }
};
```

## Design Rules

- Background: `#FFFFFF` (white)
- Cards: `#F5F5F7` (light gray)
- Modal overlay: `bg-black/40` (40% opacity)
- All text in English only
- Single light theme (no dark mode)
