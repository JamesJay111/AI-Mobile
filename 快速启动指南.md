# 🚀 Expo 快速启动指南（修复版）

## ❌ 当前问题

1. **在错误目录运行**：你在 `~/` 目录运行了命令，应该在项目目录
2. **缺少 Expo 依赖**：项目还没有安装 Expo 相关依赖

---

## ✅ 解决步骤

### 方法1：使用一键脚本（推荐）

```bash
cd ~/Desktop/AI聚合器开发方式Two
./INSTALL_AND_START.sh
```

这个脚本会：
1. 自动检查并安装 Expo 依赖
2. 启动开发服务器

### 方法2：手动安装和启动

#### 步骤1：停止当前进程
在卡住的终端按 `Ctrl + C`

#### 步骤2：切换到项目目录
```bash
cd ~/Desktop/AI聚合器开发方式Two
```

#### 步骤3：安装 Expo 依赖
```bash
npm install expo@~54.0.0 expo-router@~4.0.0 react-native@0.76.5 react-native-web@~0.19.13 expo-status-bar@~2.0.0 expo-constants@~17.0.0 expo-linking@~7.0.0 expo-splash-screen@~0.29.0 expo-system-ui@~4.0.0 react-native-safe-area-context@4.12.0 react-native-screens@~4.4.0 @react-navigation/native@^6.1.18
```

#### 步骤4：安装开发依赖
```bash
npm install --save-dev @babel/core@^7.25.0 @types/react@~18.3.0
```

#### 步骤5：启动 Expo
```bash
npx expo start
```

#### 步骤6：显示二维码
启动后，在终端按 `q` 键显示二维码

---

## 📋 完整命令（复制粘贴）

```bash
# 1. 切换到项目目录
cd ~/Desktop/AI聚合器开发方式Two

# 2. 安装 Expo 依赖
npm install expo@~54.0.0 expo-router@~4.0.0 react-native@0.76.5 react-native-web@~0.19.13 expo-status-bar@~2.0.0 expo-constants@~17.0.0 expo-linking@~7.0.0 expo-splash-screen@~0.29.0 expo-system-ui@~4.0.0 react-native-safe-area-context@4.12.0 react-native-screens@~4.4.0 @react-navigation/native@^6.1.18

# 3. 安装开发依赖
npm install --save-dev @babel/core@^7.25.0 @types/react@~18.3.0

# 4. 启动 Expo
npx expo start

# 5. 启动后按 'q' 显示二维码
```

---

## ⚠️ 重要提示

1. **必须在项目目录运行**：`~/Desktop/AI聚合器开发方式Two`
2. **首次使用需要安装依赖**：安装过程可能需要几分钟
3. **启动后按 'q'**：显示二维码用于手机扫描

---

## 🐛 如果还是卡住

### 清除缓存并重启
```bash
cd ~/Desktop/AI聚合器开发方式Two
npx expo start --clear
```

### 检查端口占用
```bash
lsof -ti:19000,19001 | xargs kill -9
```

然后重新启动：
```bash
npx expo start
```

---

**现在按以下步骤操作：**

1. 在卡住的终端按 `Ctrl + C` 停止
2. 运行：`cd ~/Desktop/AI聚合器开发方式Two`
3. 运行：`./INSTALL_AND_START.sh` 或手动安装依赖后运行 `npx expo start`
